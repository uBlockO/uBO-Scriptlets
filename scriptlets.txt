# Resources to be used as redirect destinations or for injecting java scripts.
#
# - Each distinct resource entry is separated by an empty line.
# - The first line in a resource entry is: token mime-type[;encoding]
# - All following lines are the data. An empty line signals the end of the
#   data.
#
# If the encoding is absent, the data will be converted to base64, and the
# encoding will be set to `;base64`.

# kisscartoon.*
adb.js application/javascript
window.check_adblock = function () {};

# popups/unders
od.js application/javascript
window.open = function () {};

# confirm-defuser
cd.js application/javascript
window.confirm = function () {};

# performance-defuser
pd.js application/javascript
window.performance = function () {};

# google_jobrunner-defuser
gj.js application/javascript
window.google_jobrunner = function () {};

# WebAssembly
ws.js application/javascript
window.WebAssembly = function () {};

# BetterJsPop
bp.js application/javascript
window.BetterJsPop = function () {};

# Location
lo.js application/javascript
(function () {

  var _window = window;
  var window = {};
   Object.defineProperty(window, 'location', {
      get: function () { return _window.location; },
      set: function () {}
    });
    window.__proto__ = _window;
    window.location = "blah";
}());




